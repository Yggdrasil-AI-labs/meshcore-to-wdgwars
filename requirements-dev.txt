# Development and CI dependencies (not needed at runtime — Heimdall itself is
# pure stdlib). Installed in CI to run the test suite under pytest with
# coverage, producing the coverage.xml that the SonarCloud quality gate
# consumes.
pytest>=8.0
pytest-cov>=5.0
