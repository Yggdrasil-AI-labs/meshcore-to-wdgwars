__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.venv/
venv/
.env
.DS_Store
Thumbs.db
*.swp
~$*
api.key

# Generated: web parser is copied from root heimdall.py (serve.py on startup, CI pages.yml on deploy)
web/heimdall.py

# rtk (token-killer) local state, not project content
rtk/

# Claude Code local dev config, not project content
.claude/

# RTK leftovers. The hook that wrote these was removed 2026-09-13; the
# db carries local paths and has no business in a public repo.
rtk/
